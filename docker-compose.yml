services:
  web:
    build: .
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    depends_on:
      - db
      - redis

  db:
    image: postgres:13
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: Burp33B33t
      POSTGRES_DB: gwiit_auth
    volumes:
      - gwiit_auth_data:/var/lib/postgresql/data

  redis:
    image: redis:alpine

volumes:
  postgres_data: